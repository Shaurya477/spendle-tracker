/** localStorage key for the colour theme. Read by the root layout's bootstrap script and the toggle. */
export const THEME_KEY = "penconomics-theme";

/** localStorage key for the wallet address remembered on this device (section 07). */
export const WALLET_KEY = "penconomics-wallet";
